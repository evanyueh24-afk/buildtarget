# Copy this file to `.env` for local development (with `vercel dev`), or set the
# variable in your Vercel/Netlify project settings for deployment.
#
# This key is read ONLY by the serverless function (api/chat.ts). It is never
# bundled into the client. Never commit a real key — `.env` is gitignored.

ANTHROPIC_API_KEY=
